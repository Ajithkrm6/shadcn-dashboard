import { Header } from "./index";

export default {
  title: "componnents/custom/Header",
  comonent: Header,
  parameters: { status: { type: "beta" } },
};

export const DefaultHed = () => <Header />;
